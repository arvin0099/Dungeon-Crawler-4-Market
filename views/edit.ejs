<!DOCTYPE html>
<html lang="en">
<%- include ('./partials/head.ejs')%>
<body>
    <%- include ('./partials/header.ejs')%>
    <div class="bg-cyan-800  px-4 py-8 ">
        <form action="/items/edit/<%= items._id %>?_method=PUT" method="POST">
            <div class="shadow border-2 rounded-xl bg-white p-8 size-11/12 flex-col">
                    <div class="flex flex-row items-center gap-3 p-3">
                        <label for="itemName" class=" text-gray-700 text-sm font-bold mb-2">Item Name :</label>
                        <input type="text" name="name" id="itemName" placeholder="<%= items.name %>" class="shadow appearance-none border-2 rounded w-6/12 py-2 px-3 text-gray-700"/>
                    </div>
                    <div class="flex flex-row items-center gap-3 p-3">
                        <label for="itemClass" class="block text-gray-700 text-sm font-bold mb-2">Item Class :</label>
                        <select name="class" id="itemClass" class="shadow border-2 rounded w-6/12 py-2 px-3 text-gray-700 mb-3">
                            <option value="" disabled selected><%= items.class %></option>
                            <option value="Weapon">Weapon</option>
                            <option value="Armor">Armor</option>
                        </select>
                    </div>
                    <div class="flex flex-row items-center gap-3 p-3">
                        <label for="imgUrl" class=" text-gray-700 text-sm font-bold mb-2">Image Url :</label>
                        <input type="text" name="img" id="imgUrl" placeholder="<%= items.img %>" class="shadow appearance-none border-2 rounded w-6/12 py-2 px-3 text-gray-700 "/>
                    </div>
                    <div class="flex flex-row items-center gap-3 p-3">
                        <label for="price" class=" text-gray-700 text-sm font-bold mb-2">Price :</label>
                        <input type="number" name="price" id="price" placeholder="<%=items.price%>" class="shadow appearance-none border-2 rounded w-6/12 py-2 px-3 text-gray-700 "/>
                    </div>
                    <div class="flex flex-row items-center gap-3 p-3">
                        <label for="power" class=" text-gray-700 text-sm font-bold mb-2">Power :</label>
                        <input type="number" name="power" id="power" placeholder="<%=items.power%>" class="shadow appearance-none border-2 rounded w-6/12 py-2 px-3 text-gray-700 "/>
                    <div class="flex justify-end mt-4">
                        <!-- Type="submit" took a while to debug this -->
                        <button class="shadow-md bg-red-700 hover:bg-red-800 text-white font-bold py-2 px-4 rounded" type="submit">Edit</button>
                        <button class="shadow-md bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded" type="submit" formaction="/items/delete/<%= items._id %>?_method=DELETE">Delete</button>
                     </div>
            </div>
        </form>
    </div>
</body>
</html>